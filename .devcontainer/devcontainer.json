{
  "name": "IDS FINN",
  "postStartCommand": "cd /workspaces/FINN_Networkintrusion && source env/bin/activate && pip install flask --quiet && nohup python3 dashboard.py > dashboard.log 2>&1 &",
  "forwardPorts": [5000],
  "portsAttributes": {
    "5000": {
      "label": "IDS Dashboard",
      "onAutoForward": "silent",
      "visibility": "public"
    }
  }
}